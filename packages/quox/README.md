# Quox

## Development

- Get Deno. Tested with `deno 2.5.4`.
- Get Rust. Tested with `cargo 1.90.0`.
- `deno task build` to compile the library.
- `deno publish` to upload to jsr. There is no way to use the local lib yet.
